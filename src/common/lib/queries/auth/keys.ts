const namespace = "auth";

export default {
  create: `${namespace}/create`,
  change: `${namespace}/change`,
  forgot: `${namespace}/forgot`,
  reset: `${namespace}/reset`,
  read: `${namespace}/read`,
  readAll: `${namespace}/readAll`,
};
