const namespace = "certificate-print";

export default {
  create: `${namespace}/create`,
  reupload: `${namespace}/create`,
  read: `${namespace}/read`,
  createEarly: `${namespace}/create`,
  readEarly: `${namespace}/read`,
  createKickoff: `${namespace}/create`,
  readKickoff: `${namespace}/read`,
};
